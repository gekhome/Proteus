@using System.Collections
@using Proteus.Models
@using Proteus.DAL
@model int

@(Html.Kendo().DropDownListFor(m => m)
    .AutoBind(true)
    .DataValueField("TERM_ID")
    .DataTextField("TERM")
    .OptionLabel("Επιλογή...")
    .DataSource(source =>
    {
        source.Read(read =>
        {
            read.Action("AnathesiTermsRead", "School");
        })
        .ServerFiltering(true);
    })
)
@Html.ValidationMessageFor(m => m)